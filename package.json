{
  "name": "voting",
  "version": "1.0.0",
  "description": "a voting app",
  "main": "index.js",
  "scripts": {
    "concurrent": "concurrently --kill-others --raw \"npm run start-server\" \"npm run start-client\"",
    "start-server": "cd server && npm start",
    "start-client": "cd client && npm start",
    "test-server": "cd server && npm test",
    "test-client": "cd client && npm test",
    "heroku-postbuild": "cd server && npm install && npm start; cd ../server && npm install && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/panukettu/voting.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/panukettu/voting/issues"
  },
  "homepage": "https://github.com/panukettu/voting#readme",
  "dependencies": {
    "concurrently": "^4.0.1"
  }
}
